function runPythonScript() {
    // Get the path to the Python script.
    var pythonScriptPath = "HTML_P4/ImgSearchFunction.py";
      // Run the Python script.
  subprocess.run(["python", pythonScriptPath]);
}